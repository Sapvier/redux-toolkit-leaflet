import React from 'react';
import CartIcon from "./CartIcon";
import "./styles.scss"

const Header = () => {
    return (
        <div className="header">
            <CartIcon/>
        </div>
    );
};

export default Header;